# Production requirements — CPU-only PyTorch (no CUDA, smaller install ~400 MB vs ~2 GB)
# Ultralytics/YOLO excluded — AgroNet only on production.
--extra-index-url https://download.pytorch.org/whl/cpu

python-dotenv>=1.0.0,<2
fastapi>=0.109.0,<1
uvicorn[standard]>=0.27.0,<1
python-multipart>=0.0.6
pydantic>=2.5.0,<3
opencv-python-headless>=4.8.0
numpy>=1.24.0
PyYAML>=6.0.1
google-generativeai>=0.8.0
torch>=2.1.0
torchvision>=0.16.0
