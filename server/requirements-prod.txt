# Production requirements — CPU-only PyTorch (no CUDA, no ultralytics/YOLO).
# torch + torchvision are installed separately in render.yaml buildCommand
# using --index-url https://download.pytorch.org/whl/cpu to get CPU-only wheels.

python-dotenv>=1.0.0,<2
fastapi>=0.109.0,<1
uvicorn[standard]>=0.27.0,<1
python-multipart>=0.0.6
pydantic>=2.5.0,<3
opencv-python-headless>=4.8.0
numpy>=1.24.0
PyYAML>=6.0.1
google-generativeai>=0.8.0
requests>=2.31.0
